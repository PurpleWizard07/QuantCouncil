[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "quantcouncil-mcp-server"
version = "0.1.0"
description = "Optional Phase 8 MCP server for QuantCouncil exposing ONLY paper-trading and research tools. Placeholder in foundation."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["mcp_server*"]
